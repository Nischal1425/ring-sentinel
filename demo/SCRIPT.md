# Ring Sentinel - pitch narration

GENERATED by `demo/build_frames.py` from the live pipeline. Every
number below is read back out of the detector, so editing this file
only creates drift - rerun the builder instead.

22 lines, 817 words, 4:46 of narration (measured).

Rebuild the video with:

```
python demo/build_frames.py
python demo/make_video.py
```


## The case

**`01_hero`** &nbsp; Nine merchant accounts. 129 payments. Not one of them is unusual.

**`02_reasons`** &nbsp; It only exists in what connects the accounts. And every flag says why, in plain language. A merchant can read it, and argue back.

**`05_blindspot`** &nbsp; Razorpay already scores every payment. That model is good. But it is asked one question at a time. Is this payment fine? Yes. Is this one? Yes. Nine correct answers. And the ring walks straight through. The fraud was never inside a payment. It is in what connects them.


## Why it is a ring

**`10_ring0`** &nbsp; Nine merchants. Each one, on its own, looks completely fine.

**`11_ring1`** &nbsp; Three of them sign in from the very same device. Invisible if you look at payments one at a time.

**`12_ring2`** &nbsp; And three of them share a settlement account. A marketplace shares an internet address with its sellers. It does not put them on its own settlement account.

**`13_ring3`** &nbsp; Every account was opened inside the same 69 days. Real shops open over years. Mules are onboarded in a batch.

**`14_ring4`** &nbsp; None of that is visible in any single payment. All of it is visible in the group.


## Measured on the held-out half

**`20_results`** &nbsp; Now the results, on the half it was never tuned on. 91 percent recall. 87 percent precision. But 22 rings is a small sample. The honest reading is a range. 72 to 97 percent. And score.py prints that interval beside every rate it reports.

**`21_perclass`** &nbsp; But read the per class table, not the headline. Nine classes of ring, all caught. And this one, recruiter quiet, 4 out of 5.

**`22_themiss`** &nbsp; That one scores 67. My line is 70. I could catch it by lowering the line. But two real family businesses sit at 67 and 63. Lowering it would flag them too. So it stays missed, and the reason is written down.


## The decoys

**`30_decoys`** &nbsp; I did not only plant criminals. I planted innocents, to fool myself. A franchise chain on one settlement account. A family on one device. And a brand opening six outlets in one week, which looks exactly like a batch of mules. 20 legitimate groups. 0 flagged.


## 

**`40_graph`** &nbsp; So how is that group found? Link any two merchants that share an identifier. You get 221 thousand links, and one useless blob. Because 332 of them use the same email provider. So, two rules. Drop what almost everyone shares. Make every link earn its weight. That leaves 668 links, and 47 clean groups. The largest is 9.

**`41_timing`** &nbsp; Timing is the strongest signal, and the easiest to get wrong. Everybody is busy at Diwali. So nothing is measured against a flat line. It is measured against what the book itself did that day. On day 94, this ring did 21 percent of its business. The book did half a percent. That is 39 times the rate, across 6 trading days in six months.

**`42_ratchet`** &nbsp; Then a second component argues with the first. Six questions. Is this one weak attribute? A branded chain? A marketplace? Ring R0025 scored 84. Verification cut it to 29, below the line. Nobody is troubled. Verification has no path that raises a score. The type itself rejects it.

**`43_ablate`** &nbsp; But what is each piece actually worth? Take it out, and measure again. Removing the co-timing relation costs 27 points of recall. It is the most valuable idea here. Removing the money signal costs nothing, and that row is in the table too.


## Where it loses

**`50_ml`** &nbsp; I tested whether machine learning beats the rules. It loses. And look at the tree it learned. The top split says densely connected accounts are innocent. That is backwards. It is explainable in the sense that you can print it, and what it explains is false.

**`51_elliptic`** &nbsp; I also tested on Elliptic two. Real, labelled Bitcoin subgraphs. I do not beat the published state of the art. I land at the structure only baseline. Both of those are in the repo, because a benchmark you only publish when you win is not a benchmark.


## Checks and safety

**`60_tests`** &nbsp; 17 checks. One for every claim I have made. One scrambles every identifier and proves the scores do not move. One proves the detector never opens the answer key.

**`61_safety`** &nbsp; And this is the whole action ladder. Observe. Watch. Review. And hold, a reversible one day settlement delay that expires on its own. There is no freeze in this codebase. I cannot destroy a merchant's business by accident. The function does not exist.

**`62_live`** &nbsp; And this is not a simulation. 49 orders, pulled live from the Razorpay API. 2 rings found inside them. Now read the last two lines of each. Both were refused. One attribute type, where two are required. One run, where two are required. It found them, and it still would not act.


## 

**`70_close`** &nbsp; That is Ring Sentinel. 91 percent of rings caught, on data it was never tuned on. None of the innocents flagged. And every number you have seen was re-derived from the pipeline when this video was built. The worst thing it can do to a merchant is make them wait one day.

## What is on screen

Nothing is screen-recorded. Every frame is rendered headlessly
from HTML by `demo/make_video.py`, so a frame can be reviewed one at
a time and the whole video rebuilt byte for byte. Terminal frames
show real captured output, not typed-out text.
